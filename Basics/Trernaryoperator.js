// 6.09.47 Timestamp.
// ternary operator in javascript is a concise way to writing conditional expressions. it is the only javascript operator that takes three operands: a condition followed by a question mark(?), an expression to execute if the condition is true, followed by a colon(:) , and an expression to execute if the condition is false.

// condition ? exprIfTrue : exprIfFalse

let pass = 9

let b = (pass == 8) ? true : false
console.log(b)

let person = 43

let ans = (person == 43) ? "buy products" : "They should bring money"
console.log(ans)

