const colors = ["red", "green", "blue", "yellow", "orange"]

//your task is to use array destructing to extract the first three colors from the colors array and assign them to separate vaiable named color1, color2, color3.
// After extracting the colors, log each variables value to the console.

const [color1, color2, color3] = colors
console.log(color1, color2, color3)