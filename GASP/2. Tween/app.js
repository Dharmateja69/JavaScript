// gsap  this object is the enter point for the creation of tween ans controls, in generaly contians verious method.

// what is a tween?

// gsap.method(Element,vars obj).


gsap.to(".box", {
    x: 100,//transformX(100) we can use - also
    duration: 2,
    repeat: -1
})

// generaly to method is used in gasp , which is used to send the object to one place to another.
