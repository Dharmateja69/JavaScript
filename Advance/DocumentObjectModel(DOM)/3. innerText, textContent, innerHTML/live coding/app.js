// innerText
// textContent
// innerHTML

const para = document.querySelector('p')
// para.innerText = "hi this is new world!!"
console.log(para.textContent)
console.log(para.innerHTML)
console.log(para.innerText)
